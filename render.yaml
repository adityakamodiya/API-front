services:
  - type: web
    name: client
    env: static
    buildCommand: npm run build
    startCommand: npx serve -s dist  # Use npx to ensure `serve` runs correctly
    routes:
      - src: /**
        dest: /index.html  # Redirect all requests to index.html for SPA routing
